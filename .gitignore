# Dépendances — PAS de slash initial : s'applique à tous les niveaux
# (avec "/node_modules", pos-offline/node_modules n'était pas ignoré)
node_modules/

# Sorties de build
/out
/release
pos-offline/dist/
*.tsbuildinfo

# Config vite TS compilée à la volée (fichiers horodatés jetables)
electron.vite.config.*.mjs

# Logiciel constructeur DIBAL (835 Mo) : conserve en local, jamais dans git.
# N'est PAS embarque dans le build (electron-builder.yml filtre 4 fichiers d'assets).
assets/Dossier Balance DIBAL 042021-20220331T154906Z-001/

# Secrets
.env
.env.*
!.env.example
